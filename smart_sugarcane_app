
SMART SUGARCANE PLATFORM

📱 Mobile App (Flutter)
💻 Web Admin (Flutter Web / React)
🌐 Landing Website (Canva / HTML)
☁ Backend (Firebase)
🗺 GPS + Maps (Google Maps)
📄 E-Contract (PDF + Signature)
📊 Dashboard (Realtime)
🤖 AI Analytics (Phase 2)
users/
  - admin
  - supervisor
  - driver
  - inspector

machines/
  - AE-01 ... AE-70

fields/
  - sugarcane_plots

inspections/
  - photos + GPS + notes

contracts/
  - farmer + signature + PDF

tracking/
  - realtime GPS

maintenance/
  - repair logs

reports/
  - KPI + analytics
  import 'package:flutter/material.dart';
  import 'app.dart';

  void main() {
    runApp(const SmartSugarcaneApp());
  }
  import 'package:flutter/material.dart';
  import 'features/auth/login_screen.dart';
  import 'features/dashboard/dashboard_screen.dart';

  class SmartSugarcaneApp extends StatelessWidget {
    const SmartSugarcaneApp({super.key});

    @override
    Widget build(BuildContext context) {
      return MaterialApp(
        title: 'Smart Sugarcane',
        debugShowCheckedModeBanner: false,
        theme: ThemeData(
          colorSchemeSeed: Colors.green,
          useMaterial3: true,
        ),
        initialRoute: '/login',
        routes: {
          '/login': (context) => const LoginScreen(),
          '/dashboard': (context) => const DashboardScreen(),
        },
      );
    }
  }